// Drizzle schema for photo-showcase.
//
// Intentionally empty in Phase 0 — the app scaffold and deploy skeleton come
// first. The real tables (clients, packages, galleries, assets) plus NextAuth's
// adapter tables land in Phase 2, when the domain model is built. See PLAN.md §6.

export {};
